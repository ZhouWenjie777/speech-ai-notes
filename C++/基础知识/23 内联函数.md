---
data: 2025-12-16T18:30:00
tags:
---
1. 内联函数
	- 概念：在编译阶段用函数体代码替换函数调用，消除调用开销，适用于短小、频繁调用的函数。
	- 格式：inline 返回类型 函数名(形参表) { 函数体 }，定义必须出现在调用之前，否则无法展开。
	- 示例：inline double square(double x) { return x * x; }，调用square(5.0)被直接替换为5.0 * 5.0。
	- 注意：函数体过大或含复杂控制结构时编译器可能拒绝内联；递归函数无法内联