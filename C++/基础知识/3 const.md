---
data: 2025-12-15T16:02:00
tags:
---
1. 符号常量
	- 定义：给常量起名字，一改全改，提高可读性
	- 旧写法：`#define PI 3.1416` 纯文本替换，无类型检查，不推荐
	- 新写法：`const double PI = 3.1416;` 有类型、有作用域、受编译器检查
	- 命名惯例：全大写或首字母大写，如`MAX_SIZE`、`TaxRate`，与变量区分
	- 规则：const对象必须初始化，之后不可修改；可作用于任何类型