---
data: 2025-12-16T18:29:00
tags:
---
1. 指向函数的指针
	- 声明：返回类型 (\*指针变量名)(形参表)；例 `int (*p)(int,int);` 括号不可省，否则变返回指针的函数
	- 赋值：`p = isdigit;` 函数名自动退化成地址，也可 `p = &isdigit;` 两者等价。
	- 调用：`(*p)(n,k) 或 p(n,k)` 均可，后者语法糖，效果相同
	- 应用：作函数参数实现“回调”，如 estimate(lines, pf) 接收程序员自定义估算算法，提高通用性与扩展性
